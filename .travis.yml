language: node_js
node_js:
 - "stable"
services:
  - docker
before_install:
  - docker build -t image-sequencer-app .
  - docker-compose build
  - docker run -p 4000:4000 image-sequencer-app

script: npm test
install:
 - npm install
os:
  - linux
  - osx
